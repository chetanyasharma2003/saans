services:
  - type: web
    name: saans-api
    env: node
    plan: standard
    region: ohio
    buildCommand: bash render-build.sh
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NPM_CONFIG_LEGACY_PEER_DEPS
        value: "true"
